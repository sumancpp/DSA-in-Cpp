#include <iostream>
using namespace std;
int main (){
   int a = 3, b = 7;
    //    cout<<(a & b);
    //    cout<<(a | b);
    //   cout<<(a ^ b);
    // cout<<(10 << 2);
    cout<<(10 >> 1);

    // bitwise << ans:- a * 2^b
    // bitwise >> ans:- a / 2^b

    /*
    Operator Priority
    !, +, -       R to L
    *, /, %
    +, -
    <, <=, >=, >
    ==, !=
    &&
    ||
    =
    
    */
    return 0;
}